# taller3-desarrollo-Ramses-Rodriguez
primer problema de lista de compra inteligente

1. que estructura de datos usaste?
   como principal estructura se utilizo el ArrayList para almacenar los datos, tantos los agregados y con los que puede agregar el usuario
   la lsita se de fine como String que es para agregar carecteres/letras.

2. como agregaste un nuevo producto?
   se utilizo una switch donde se da la opcion de agregar producto de a la lista/ArrayList donde se da el nombre de la lista y se usa add(articulo) para agregar.

3. como verificaste si un producto existe?
   se utiliso un caso donde ser le da la opcion de buscar un producto, como tambien se utilizo otro caso donde se puede mostrar la lista, con el caso donde se puede buscar el articulo/producto, te dice si existe o no existe en la lista.
